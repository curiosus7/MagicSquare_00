# MagicSquare_00 — 세션 04 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_00 |
| 세션 | 04 — Harness · `.cursorrules` · Commands · TDD RED |
| 보고서 생성일 | 2026-06-10 |
| 범위 | pytest Harness · 규칙 문서 · Cursor Commands · `validate_lines` RED 1건 |

---

## 1. 요약

세션 3 `validate_lines` 구현을 위한 **최소 Harness**를 구축하고, **`.cursorrules`**로 도메인·API·TDD 규칙을 고정했다. Harness는 `MAGIC_CONSTANT`·`LINE_IDS`·`LINE_CELLS` SSOT를 `src/validate_lines.py`에 두었다. **`.cursor/commands/tdd-red.md`**로 RED 절차를 Command화했고, **`/tdd-red`** 실행으로 `incomplete`(0 포함) 테스트 1건을 추가해 **pytest FAILED**를 확인했다. **`.cursor/commands/export-session.md`**로 세션 Export 절차를 추가했다. 다음 단계는 **GREEN** — `validate_lines` incomplete 최소 구현.

---

## 2. 산출물

| 파일 | 설명 |
|------|------|
| `pyproject.toml` | pytest 설정 (`testpaths`, `pythonpath`) |
| `src/validate_lines.py` | SSOT 상수 + `validate_lines` 스tub (`...`) |
| `tests/test_validate_lines.py` | RED 테스트 1건 |
| `.cursorrules` | 도메인 · API · TDD · AI 행동 규칙 |
| `.cursor/commands/tdd-red.md` | RED 전용 Command (AAA · 금지 · 보고 형식) |
| `.cursor/commands/export-session.md` | Report + Transcript Export Command |

---

## 3. TDD / 작업 기록

| Phase | 내용 | pytest / 결과 |
|-------|------|----------------|
| **RED** | `test_incomplete_when_grid_has_zero` — 0 포함 격자 → `incomplete`, `failed_lines=[]` | **FAILED** — `TypeError: 'NoneType' object is not subscriptable` (미구현 `...`) |
| GREEN | *(미진행)* | — |
| REFACTOR | *(미진행)* | — |

**RED 명령:** `pytest tests/test_validate_lines.py -v` → 1 failed

---

## 4. 결정·규칙

- **API:** `validate_lines(grid)` → `{status: pass\|fail\|incomplete, failed_lines: [str, ...]}`
- **incomplete:** 0 하나라도 있으면 합 검증 생략, `failed_lines=[]`
- **10선:** R1~R4, C1~C4, D1(주↘), D2(반↙) — `LINE_IDS`·`LINE_CELLS` SSOT
- **마법상수:** `MAGIC_CONSTANT = 34` (`src/validate_lines.py`), 리터럴 34 금지
- **TDD:** RED → GREEN → REFACTOR; RED는 `tests/`만; assert 완화·skip·xfail 금지
- **Export:** `Report/NN.REPORT.md` + `Prompting/NN.Export-Transcript.md` (`NN.XXX` 형식)

---

## 5. 다음 단계

- **GREEN:** `src/validate_lines.py` — 0 포함 시 `{status: "incomplete", failed_lines: []}` 최소 구현
- 이후 RED 추가: `fail`(합≠34), `pass`(정답 격자) 시나리오

---

## 6. 관련 문서

| 문서 | 경로 |
|------|------|
| Transcript | `Prompting/04.Export-Transcript.md` |
| 규칙 | `.cursorrules` |
| TDD RED Command | `.cursor/commands/tdd-red.md` |
| Export Command | `.cursor/commands/export-session.md` |
| 세션 3 워크북 | `Report/03.REPORT.md` |
